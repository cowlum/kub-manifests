apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  namespace: development
data:
  bob: "This is the content of the file bob."
  ENVIRONMENT: "development"
  LOG_LEVEL: "warn"
  init-script.sh: |
    #!/bin/sh
    touch /etc/bober
